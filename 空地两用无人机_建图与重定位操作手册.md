# 空地两用无人机建图与重定位操作手册

版本：2026-08-27  
适用设备：当前 SpiritWing 空地两用无人机实机  
远程主机：`bitcq@192.168.50.102`  
ROS 工作空间：`/home/bitcq/catkin_ws`  
已验证地图：`site_20260826_01`

## 1. 使用范围

本文说明三项按先后依赖关系已经实机跑通的工作：

1. 使用遥控器驾驶地面模态，利用 Livox + FAST-LIO 手动建图并保存地图包。
2. 机器人重新进入已建地图时，在完全未知初始位姿条件下进行全局重定位。
3. 重定位有效后，在历史地图中执行地面模态单点自主导航和实时避障。

本文不包含有序多任务点、自动起飞、自动降落和飞行自主导航验收。建图、地图加载和重定位过程本身不会主动解锁、起飞、降落或发送导航速度；只有操作者显式下发目标并解除软件急停后，地面执行器才允许动作。

## 2. 安全要求

- 操作区域内不得有人靠近螺旋桨和车轮。
- 建图期间只使用遥控器驾驶，不启动 ROS 软件底盘控制、导航或任务执行器。
- 切换遥控器模式、解锁或地面/飞行机械模态时，以设备现有安全规程为准。
- 全局重定位计算期间保持机器人静止；首次定位确认前不得解除软件急停或下发运动许可。
- 同一时间只能运行一套 Livox、FAST-LIO 和 TF 发布链。切换“建图”和“重定位”前必须先用 `Ctrl+C` 停止旧 launch。
- `/ground_air/localization/valid` 不是 `True` 时，禁止启动自主任务。
- 如出现异常运动，立即使用遥控器接管；若控制栈已启动，可调用紧急停止服务，但不要把软件服务作为唯一安全手段。

## 3. 开机和连接

建议顺序：

1. 确认机器人周围安全、轮子和桨叶无阻挡。
2. 打开 H16 PRO 遥控器，确认电量充足。
3. 给机器人上电，等待机载计算机、飞控、雷达和天空端启动。
4. 连接 GroundControlStation，确认能够看到飞控连接和电量。
5. 在 Windows PowerShell 或 VS Code 中连接远程主机：

```powershell
ssh bitcq@192.168.50.102
```

密码为已约定的单个空格字符。登录后进入工作空间：

```bash
cd ~/catkin_ws
source /opt/ros/noetic/setup.bash
source devel/setup.bash
```

## 4. 手动建图

### 4.1 启动建图系统

在远程终端一执行：

```bash
cd ~/catkin_ws
source /opt/ros/noetic/setup.bash
source devel/setup.bash
roslaunch car_bringup manual_mapping.launch
```

等待 Livox、FAST-LIO、动态栅格地图和地图记录器启动。不要在同一终端继续输入其他命令。

### 4.2 检查关键输入

打开远程终端二：

```bash
cd ~/catkin_ws
source /opt/ros/noetic/setup.bash
source devel/setup.bash
```

检查注册点云：

```bash
rostopic echo -n1 /cloud_registered_1/header
```

预期：

```text
frame_id: "camera_init"
```

检查 FAST-LIO 里程计：

```bash
rostopic echo -n1 /Odometry_loc/header
```

预期同样为：

```text
frame_id: "camera_init"
```

如本次建图需要 PX4 Position 模式，再检查送给 MAVROS/PX4 的视觉里程计：

```bash
rostopic echo -n1 /mavros/odometry/out/header
rostopic echo -n1 /mavros/state
```

只有上述输入持续更新、飞控状态正常时，才按设备既有流程操作遥控器。若遥控器 CH5/CH6 不变化，先检查 H16 输出模式、接收链路和 `/mavros/rc/in`，不要反复尝试解锁。

### 4.3 开始记录地图

每次使用一个新的 `map_id`。允许字符为字母、数字、点、下划线和连字符，最长 64 字符。同名地图不会覆盖。

示例：

```bash
rosservice call /ground_air/mapping/start "map_id: 'site_20260826_01'"
```

成功标志：

```text
success: True
state: 1
message: "recording registered point cloud and occupancy map"
```

`state: 1` 表示已经开始记录。刚开始时 `point_count: 0` 是正常的，随后会增加。

### 4.4 观察建图状态

```bash
rostopic echo /ground_air/mapping/status
```

状态值：

| 数值 | 状态 | 含义 |
|---:|---|---|
| 0 | IDLE | 空闲 |
| 1 | RECORDING | 正在记录 |
| 2 | SAVING | 正在保存 |
| 3 | COMPLETE | 保存完成 |
| 4 | ERROR | 发生错误 |

也可以查看点云和栅格地图是否存在：

```bash
rostopic hz /cloud_registered_1
rostopic hz /map
```

### 4.5 遥控器驾驶建图

- 使用地面模态低速行驶。
- 起步和转弯尽量平缓，避免急加速、急停和原地高速旋转。
- 沿区域边界行驶，再覆盖内部通道。
- 经过墙角、门口、立柱等明显结构。
- 相邻轨迹保留足够重叠，不要只走单条直线。
- 最好形成闭环并回到起点附近。
- 建图过程中不要关闭雷达、FAST-LIO 或 ROS launch。

### 4.6 保存地图

驾驶完成后先停车并保持静止，然后执行：

```bash
rosservice call /ground_air/mapping/save "{}"
```

保存成功应返回：

```text
success: True
map_directory: "/home/bitcq/catkin_ws/maps/<map_id>"
point_count: <大于0>
```

检查地图文件：

```bash
ls -lh /home/bitcq/catkin_ws/maps/<map_id>/
```

完整地图包应包含：

```text
cloud_map.pcd     三维点云地图
map.pgm           二维栅格地图图像
map.yaml          栅格地图参数
metadata.json     地图元数据
```

确认文件完整后，再在建图 launch 所在终端按 `Ctrl+C`。

### 4.7 取消未保存的建图

仅在确定不要本次未保存缓存时执行：

```bash
rosservice call /ground_air/mapping/cancel "{}"
```

取消会清理当前未保存缓存，不会删除已经保存的历史地图包。

### 4.8 将地图复制到 Windows 桌面

在 Windows PowerShell 中执行，将 `<map_id>` 替换为实际名称：

```powershell
scp bitcq@192.168.50.102:/home/bitcq/catkin_ws/maps/<map_id>/map.pgm "$env:USERPROFILE\Desktop\"
scp bitcq@192.168.50.102:/home/bitcq/catkin_ws/maps/<map_id>/cloud_map.pcd "$env:USERPROFILE\Desktop\"
scp bitcq@192.168.50.102:/home/bitcq/catkin_ws/maps/<map_id>/map.yaml "$env:USERPROFILE\Desktop\"
```

`map.pgm` 可用普通图片工具查看；`cloud_map.pcd` 可使用 CloudCompare、Open3D 或 PCL 工具查看。

## 5. 未知初始位姿重定位

### 5.1 测试前准备

1. 停止 `manual_mapping.launch` 和其他 Livox/FAST-LIO launch。
2. 将机器人放在已建地图覆盖区域内。
3. 第一次优先选择结构明显且离建图起点较近的位置。
4. 雷达周围无遮挡，机器人保持静止。

### 5.2 启动仅定位测试栈

在远程终端一执行：

```bash
cd ~/catkin_ws
source /opt/ros/noetic/setup.bash
source devel/setup.bash

roslaunch car_bringup ground_air_full.launch \
  start_mavros:=false \
  start_livox:=true \
  start_fast_lio:=true \
  start_fastlio_odometry_to_px4:=false \
  start_ground_filter:=false \
  start_dynamic_mapping:=false \
  start_localization:=true \
  start_navigation:=false \
  start_control:=false \
  start_mission:=false \
  start_platform:=false \
  start_vision_to_mavros:=false \
  start_fusion_client:=false \
  start_video:=false
```

该启动方式只提供 Livox、FAST-LIO、地图管理、全局重定位和全局 TF，不会启动运动控制。

### 5.3 检查定位输入

在远程终端二执行：

```bash
cd ~/catkin_ws
source /opt/ros/noetic/setup.bash
source devel/setup.bash

rostopic echo -n1 /cloud_registered_1/header
rostopic echo -n1 /Odometry_loc/header
```

两者必须持续更新，且 `frame_id` 均应为 `camera_init`。

可选频率检查：

```bash
timeout 8 rostopic hz /cloud_registered_1
timeout 8 rostopic hz /Odometry_loc
```

### 5.4 加载历史地图

```bash
rosservice call /ground_air/load_map "map_id: 'site_20260826_01'
source_uri: ''"
```

预期返回：

```text
success: True
message: "map activated; relocalization required"
```

确认二维地图已发布：

```bash
rostopic echo -n1 /map/header
```

预期：

```text
frame_id: "world"
```

### 5.5 执行完全未知位姿重定位

```bash
rosservice call /ground_air/relocalize "use_initial_guess: false
initial_guess: {}
timeout: 60.0"
```

服务最多运行 60 秒。计算期间保持机器人静止。

成功返回示例：

```text
success: True
message: "global relocalization confirmed on consecutive scans"
pose.header.frame_id: "world"
fitness: 0.99...
rmse: 0.04...
```

当前成功门限：

- `fitness >= 0.55`
- `rmse <= 0.30 m`
- 连续两次结果满足门限
- 两次结果差异不超过 `0.50 m / 0.35 rad`

### 5.6 验证结果和跟踪状态

```bash
rostopic echo -n1 /ground_air/localization/valid
rostopic echo -n1 /ground_air/localization/fitness
rostopic echo -n1 /ground_air/localization/rmse
rostopic echo -n1 /ground_air/localization/pose
```

本次成功必须同时满足：

```text
valid = True
fitness >= 0.55
rmse <= 0.30
pose.header.frame_id = world
```

检查全局 TF：

```bash
rosrun tf tf_echo world camera_init
```

重定位成功后应持续输出 `world -> camera_init`。成功前该变换不发布。

当前已经验证的一次实机结果为：

```text
position ≈ (0.446, -0.673, 0.000) m
fitness ≈ 1.0
rmse ≈ 0.041 m
valid = True
```

### 5.7 重复性验收

单次成功只表示当前位置重定位成功，建议继续完成：

1. 在同一标记位置重启定位栈并重复 3 次。
2. 三次位姿离散不超过 `0.30 m / 5°`。
3. 将机器人移动 2～5 米并改变 30°～90°朝向。
4. 重启定位栈，重新加载地图并执行未知位姿重定位。
5. 只有 `valid=True` 后才能进入低速自主导航测试。

## 6. 从重定位递进到地面单点自主导航

本章必须在第 5 章完成且定位健康后执行。当前已实机验证：历史地图加载、未知位姿重定位、单点全局规划、TEB 局部避障、低速地面执行和软件急停闭环。

### 6.1 启动定位与平台基础栈

如果此前按第 5.2 节启动了“仅定位测试栈”，先在其终端按 `Ctrl+C` 完全停止，再执行本节命令；不得同时启动两套 Livox、FAST-LIO、定位或 TF 节点。

在终端一执行：

```bash
cd ~/catkin_ws
source /opt/ros/noetic/setup.bash
source devel/setup.bash

roslaunch car_bringup autonomy.launch \
  start_mavros:=true \
  start_navigation:=false \
  start_control:=false \
  start_mission:=false \
  start_video:=false
```

完成第 5.4～5.6 节的地图加载、全局重定位和质量检查。每次重新开机、切换地图或机器人被搬动后，都必须重新执行全局重定位，不能沿用旧位姿。

### 6.2 启动规划与避障

在终端二执行：

```bash
cd ~/catkin_ws
source /opt/ros/noetic/setup.bash
source devel/setup.bash

roslaunch teb_local_planner_tutorials bz_navigation.launch \
  enable_drone_mode:=false
```

检查当前保守实机参数：

```bash
rosparam get /move_base/recovery_behavior_enabled
rosparam get /move_base/clearing_rotation_allowed
rosparam get /move_base/TebLocalPlannerROS/max_vel_x
rosparam get /move_base/TebLocalPlannerROS/max_vel_theta
```

预期：

```text
false
false
0.1
0.2
```

不可规划时应停车，不允许启用 `move_base` 的高速旋转恢复。

确认避障输入持续更新：

```bash
timeout 8 rostopic hz /cloud_registered_body_1
rostopic echo -n1 /move_base/local_costmap/costmap/header
```

### 6.3 启动地面控制并确认启动急停

在终端三执行：

```bash
cd ~/catkin_ws
source /opt/ros/noetic/setup.bash
source devel/setup.bash

roslaunch ground_air_control control.launch
```

控制栈启动时自动锁定软件急停。检查：

```bash
rostopic echo -n1 /ground_air/vehicle_status
rostopic echo -n1 /mavros/actuator_control
```

必须满足：

```text
emergency_stop: True
controls: 全部为 0
```

然后使用遥控器按设备安全流程切换到地面构型、`POSCTL` 并解锁。禁止使用 ROS 服务代替遥控器解锁。确认：

```bash
rostopic echo -n1 /mavros/state
rostopic echo -n1 /mavros/extended_state
```

预期为 `connected=True`、`armed=True`、`mode="POSCTL"`、地面构型 `vtol_state=4`。

### 6.4 目标点检查

目标坐标必须位于当前加载地图的已知自由区域。下发前：

1. 查看 `/ground_air/localization/pose`，确认地图航向与真实车头一致。
2. 使用 `/move_base/make_plan` 做 `tolerance: 0.0` 的精确终点检查。
3. 精确终点无路径、处于未知区、墙体或膨胀区时，不得解除急停。
4. 首次测试只选择 `0.5～1.0 m` 的短距离目标，不直接使用 6 m 长距离目标。

查看当前位姿：

```bash
rostopic echo -n1 /ground_air/localization/pose
```

将当前位置填入 `<当前X>/<当前Y>`、目标填入 `<目标X>/<目标Y>`，执行零容差规划检查：

```bash
rosservice call /move_base/make_plan "
start:
  header: {frame_id: 'world'}
  pose:
    position: {x: <当前X>, y: <当前Y>, z: 0.0}
    orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}
goal:
  header: {frame_id: 'world'}
  pose:
    position: {x: <目标X>, y: <目标Y>, z: 0.0}
    orientation: {x: 0.0, y: 0.0, z: <目标QZ>, w: <目标QW>}
tolerance: 0.0"
```

响应中的 `plan.poses` 必须非空；出现 `poses: []` 时保持急停并重新选择目标。

### 6.5 下发单点目标

软件急停保持 `True` 时发布目标：

```bash
rostopic pub -1 /move_base_simple/goal geometry_msgs/PoseStamped "
header:
  frame_id: 'world'
pose:
  position: {x: <目标X>, y: <目标Y>, z: 0.0}
  orientation: {x: 0.0, y: 0.0, z: <目标QZ>, w: <目标QW>}"
```

若不需要到点后额外调头，可将目标 `QZ/QW` 设为当前定位姿态的 `z/w`。检查 `/move_base/status` 已接受目标后，才解除软件急停：

```bash
rosservice call /ground_air/emergency_stop "active: false"
```

成功响应必须包含：

```text
success: True
message: "operator reset; ground OFFBOARD confirmed"
```

该服务先确认 PX4 已进入 `OFFBOARD`，再允许导航速度进入地面执行器。如果返回 `False`，禁止重复盲目调用，应重新激活急停并排查。

### 6.6 运行监控与到点停车

重点监控：

```bash
rostopic echo /ground_air/localization/valid
rostopic echo /ground_air/vehicle_status
rostopic echo /move_base/status
rostopic echo /navigation/cmd_vel
```

`move_base` 常用状态：`1=执行中`、`3=成功到达`、`4=规划失败`。定位失效、飞控退出 `OFFBOARD`、规划失败、车辆方向异常或出现非预期高速旋转时，立即急停。

到达后执行：

```bash
rosservice call /ground_air/emergency_stop "active: true"
```

控制栈会先阻断速度、等待执行器归零，再将 PX4 恢复为 `POSCTL`。确认 `emergency_stop=True`、`flight_mode="POSCTL"` 且执行器全零。

### 6.7 中途取消与紧急停止

正常取消：

```bash
rostopic pub -1 /move_base/cancel actionlib_msgs/GoalID "{}"
rosservice call /ground_air/emergency_stop "active: true"
```

紧急情况先使用遥控器安全开关，再执行：

```bash
rosservice call /ground_air/emergency_stop "active: true"
rostopic pub -1 /move_base/cancel actionlib_msgs/GoalID "{}"
```

必须取消旧目标，避免以后解除急停时旧目标继续执行。

### 6.8 当前验收边界

截至 2026-08-27，地面模态单点自主导航和实时避障已由操作者实机确认可用。以下仍需后续验收：

- 一组有序任务点依次执行并在每点停留 2 秒。
- 指控平台任务下发与状态闭环。
- 任务过程中的地面/飞行模态切换。
- 自动起飞、定高飞行、自主飞行避障和按指令降落。

## 7. 常见故障

### `no active point-cloud map`

原因：尚未调用 `/ground_air/load_map`。先加载地图，再重定位。

### `timed out waiting for a fresh registered point cloud`

检查：

```bash
rostopic hz /cloud_registered_1
```

确认 Livox 和 FAST-LIO 正常运行。

### `registered cloud frame ... expected camera_init`

检查：

```bash
rostopic echo -n1 /cloud_registered_1/header
```

不要用错误 frame 的点云替代 `/cloud_registered_1`。

### `registration quality or cross-scan consistency check failed`

可能原因：当前环境与历史地图重叠不足、场景过于对称、雷达被遮挡或机器人在计算中移动。移动到结构更明显且有足够地图重叠的位置后重试。

### `valid` 由 `True` 变成 `False`

定位跟踪连续失败 3 次，或点云超过 2 秒未更新时会自动失效。停止自主任务，检查点云与 FAST-LIO，重新加载地图并重定位。

### `ground_air_map_manager` 退出

当前已修复 Python 模块导入问题。如果再次出现，查看：

```bash
roscd ground_air_localization
roslaunch ground_air_localization localization.launch
```

并保存终端中的完整错误以及对应 ROS 日志路径。

### `Unable to load type [ground_air_msgs/...]`

当前终端没有加载新工作空间。执行：

```bash
source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash
```

再用 `rossrv show ground_air_msgs/SetEmergencyStop` 验证。每个新终端都必须重新执行两条 `source` 命令。

### 导航时高速原地转圈

立即使用遥控器和软件急停停止车辆，然后取消目标。检查：

```bash
rosparam get /move_base/recovery_behavior_enabled
rosparam get /move_base/clearing_rotation_allowed
```

两者必须为 `false`。如果修改的是 launch 文件，必须重启 `move_base` 才会生效；只修改已运行进程的参数服务器不足以卸载已经初始化的旋转恢复插件。

### 单点目标已发布但规划失败

常见原因是终点处于历史地图边界、未知区、墙体或障碍膨胀区。即使物理现场看起来开阔，也必须以历史地图和全局代价地图为准。重新定位后核对车头航向，并使用 `tolerance: 0.0` 重新检查精确目标；无路径时保持急停，不要用较大容差强行接受邻近目标。
